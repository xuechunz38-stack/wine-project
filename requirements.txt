# Pinned to the versions the README results were produced with, so a fresh
# install (locally or in CI) reproduces the same numbers.
numpy==2.0.2
pandas==2.3.3
polars==1.36.1
scikit-learn==1.6.1
matplotlib==3.9.4
pytest==8.3.5
